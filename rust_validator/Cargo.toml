[package]
name = "rust_validator"
version = "0.1.0"
edition = "2021"
description = "Rust library for energy data validation, integrated with Go via cgo"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
